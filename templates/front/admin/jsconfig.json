{
  "compilerOptions": {
    // "target": "ES6",
    "paths": {
      // @ 作为 src目录别名
      "@/*": ["./src/*"] // VS Code 将 `@` 解析为 `src` 目录
    }
  },
  // "exclude": ["node_modules", "dist"], // "...其他要排除的目录"
  // "include": ["src/**/*"]
}
